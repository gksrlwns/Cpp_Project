# Day 2
```
Ctrl + , : 코드 검색
// 사용자가 직접 값을 입력하는 방법
	
	// 입력 : scanf_s 라는 함수를 통해서 진행
	// 사용법 : scanf_s("%d", &변수명);
	//  => 변수안에 사용자가 입력한 값이 들어갑니다.
	
	// 문자열 입력만 조금 특이합니다!
	// scanf_s("%s", 변수명, (int)sizeof(변수명));
```